NAME

_printf

SYNOPSIS

#include <stdio.h>
int _printf(const char *format, ...);

DESCRIPTION

The functions in the _printf() function produces output according to a format._printf() functions  write output to the given output stream.

EXAMPLES
 To print a date and time in the form ``Sunday, July 3, 10:02'', where
     weekday and month are pointers to strings:

           #include <stdio.h>
           _printf("%s, %s %d, %.2d:%.2d\n",
                   weekday, month, day, hour, min);
SEE ALSO
       printf(3)

       Full documentation at: <https://www.gnu.org/software/coreutils/printf>
       or available locally via: info '(coreutils) printf invocation'

AUTHOR
	Written by Murage Brendan and Alazar Gebrekirstos.

COPYRIGHT
  Copyright 2021 Alazar and Murage, Alx.
  There is NO WARRANTY, to the extent permitted by law.
  This is an attempt a man page to a custonm _printf().